version: 0.0
os: linux
files:
  - source: /
    estination: /home/ec2-user/app # 경로
    overwrite: yes
permissions:
  - object: /home/ec2-user/app
    pattern: "**"
    owner: root
    group: root
hooks:
  AfterInstall:
    - location: scripts/deploy.sh
      timeout: 60
      runas: root